import { LocationDirective } from './location.directive';

describe('LocationDirective', () => {
  it('should create an instance', () => {
    const directive = new LocationDirective();
    expect(directive).toBeTruthy();
  });
});
